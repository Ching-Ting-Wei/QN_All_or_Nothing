#ifndef __PATHGRAPH_H
#define __PATHGRAPH_H
#include <iostream>
#include <vector>
#include <map>
#include <fstream>
#include "../../config.h"
using namespace std;

class PathGraph
{ 
/*
    int num_of_path;
    int k;
    // parameter for generating PathGraph
    vector<vector<int> > neighbor;
public:
    // PathGraph(vector<Node> nodes, map<node, vector<Edge>> edges);
    PathGraph( int num_of_node, vector<double>alpha, vector<vector<int> > neighbor);
    ~PathGraph(void);
    int get_size();                                        // return |V|
    vector<int> get_neighbors_id(int node1_id);            // int get_neighbor_size(int node_id); int get_neighbor_id(int node1_id, int nth);
    pair<int,int> pos_trans(int id);
*/   
};

#endif